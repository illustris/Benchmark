#include <stdint.h>

void show_help();
void close_this(int, char *);
void print_hash(uint8_t *);
int s_len(char *);
short is_breakable(char *);
short compare(uint8_t *, uint8_t *);
char *get_help_text();
